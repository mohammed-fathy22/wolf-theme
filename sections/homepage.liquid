<!-- Hero Section -->
<section class="relative bg-gradient-to-r from-blue-600 to-purple-700 text-white">
  <div class="absolute inset-0 bg-black opacity-20"></div>
  <div class="relative mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
    <div class="text-center">
      <h1 class="text-4xl font-bold tracking-tight sm:text-6xl">
        {{ section.settings.hero_title | default: 'Welcome to ' | append: shop.name }}
      </h1>
      <p class="mx-auto mt-6 max-w-2xl text-lg leading-8 text-blue-100">
        {{
          section.settings.hero_description
          | default: 'Discover amazing products and exceptional quality. Shop with confidence and enjoy fast, reliable delivery.'
        }}
      </p>
      <div class="mt-10 flex items-center justify-center gap-x-6">
        <a
          href="{{ section.settings.hero_button_url | default: collections.all.url }}"
          class="rounded-md bg-white px-6 py-3 text-sm font-semibold text-gray-900 shadow-sm hover:bg-gray-50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
        >
          {{ section.settings.hero_button_text | default: 'Shop Now' }}
        </a>
        <a
          href="{{ section.settings.hero_secondary_button_url | default: pages.about.url }}"
          class="text-sm font-semibold leading-6 text-white hover:text-blue-100"
        >
          {{ section.settings.hero_secondary_button_text | default: 'Learn more' }}
          <span aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </div>
</section>

<!-- Featured Collections -->
{% if section.settings.show_featured_collections %}
  <section class="py-16 bg-gray-50">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
          {{ section.settings.featured_collections_title | default: 'Shop by Category' }}
        </h2>
        <p class="mx-auto mt-4 max-w-2xl text-lg text-gray-600">
          {{ section.settings.featured_collections_description | default: 'Explore our curated collections' }}
        </p>
      </div>

      <div class="mt-12 grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
        {% for collection in collections limit: 6 %}
          {% unless collection.handle == 'all' %}
            <div class="group relative">
              <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-lg bg-gray-200 lg:aspect-none group-hover:opacity-75 lg:h-80">
                {% if collection.image %}
                  <img
                    src="{{ collection.image | image_url: width: 400 }}"
                    alt="{{ collection.title | escape }}"
                    class="h-full w-full object-cover object-center lg:h-full lg:w-full"
                    width="400"
                    height="320"
                    loading="lazy"
                  >
                {% else %}
                  <div class="flex h-full w-full items-center justify-center bg-gray-200">
                    <span class="text-4xl text-gray-400">{{ collection.title | first }}</span>
                  </div>
                {% endif %}
              </div>
              <div class="mt-4 flex justify-between">
                <div>
                  <h3 class="text-lg font-medium text-gray-900">
                    <a href="{{ collection.url }}">
                      <span aria-hidden="true" class="absolute inset-0"></span>
                      {{ collection.title }}
                    </a>
                  </h3>
                  <p class="mt-1 text-sm text-gray-500">{{ collection.products_count }} products</p>
                </div>
              </div>
            </div>
          {% endunless %}
        {% endfor %}
      </div>
    </div>
  </section>
{% endif %}

<!-- Featured Products -->
{% if section.settings.show_featured_products %}
  <section class="py-16">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
          {{ section.settings.featured_products_title | default: 'Featured Products' }}
        </h2>
        <p class="mx-auto mt-4 max-w-2xl text-lg text-gray-600">
          {{ section.settings.featured_products_description | default: 'Check out our best-selling items' }}
        </p>
      </div>

      <div class="mt-12 grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-4">
        {% for product in collections[section.settings.featured_collection].products limit: 8 %}
          <div class="group relative">
            <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-lg bg-gray-200 lg:aspect-none group-hover:opacity-75 lg:h-80">
              {% if product.featured_image %}
                <img
                  src="{{ product.featured_image | image_url: width: 300 }}"
                  alt="{{ product.title | escape }}"
                  class="h-full w-full object-cover object-center lg:h-full lg:w-full"
                  width="300"
                  height="240"
                  loading="lazy"
                >
              {% else %}
                <div class="flex h-full w-full items-center justify-center bg-gray-200">
                  <span class="text-4xl text-gray-400">No Image</span>
                </div>
              {% endif %}
            </div>
            <div class="mt-4 flex justify-between">
              <div>
                <h3 class="text-sm text-gray-700">
                  <a href="{{ product.url }}">
                    <span aria-hidden="true" class="absolute inset-0"></span>
                    {{ product.title }}
                  </a>
                </h3>
                <p class="mt-1 text-sm font-medium text-gray-900">
                  {% if product.compare_at_price > product.price %}
                    <span class="text-red-600">{{ product.price | money }}</span>
                    <span class="ml-2 text-sm text-gray-500 line-through">{{ product.compare_at_price | money }}</span>
                  {% else %}
                    {{ product.price | money }}
                  {% endif %}
                </p>
              </div>
            </div>
          </div>
        {% endfor %}
      </div>

      <div class="mt-12 text-center">
        <a
          href="{{ collections.all.url }}"
          class="inline-flex items-center rounded-md border border-transparent bg-gray-900 px-6 py-3 text-base font-medium text-white shadow-sm hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2"
        >
          View All Products
        </a>
      </div>
    </div>
  </section>
{% endif %}

<!-- Features Section -->
{% if section.settings.show_features %}
  <section class="py-16 bg-gray-50">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
          {{ section.settings.features_title | default: 'Why Choose Us' }}
        </h2>
        <p class="mx-auto mt-4 max-w-2xl text-lg text-gray-600">
          {{ section.settings.features_description | default: 'We provide exceptional service and quality products' }}
        </p>
      </div>

      <div class="mt-12 grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
        <div class="text-center">
          <div class="mx-auto flex h-12 w-12 items-center justify-center rounded-md bg-blue-500 text-white">
            <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 18.75a1.5 1.5 0 01-3 0m3 0a1.5 1.5 0 00-3 0m3 0h6m-9 0H3.375a1.125 1.125 0 01-1.125-1.125V14.25m6.75 4.5v-4.5m0 0H9.375a1.125 1.125 0 01-1.125-1.125V9.75m6.75 4.5H9.375a1.125 1.125 0 01-1.125-1.125V9.75m6.75 4.5v-4.5m0 0h6.75a1.125 1.125 0 011.125 1.125v4.5m-6.75 0v4.5" />
            </svg>
          </div>
          <h3 class="mt-4 text-lg font-semibold text-gray-900">Fast Shipping</h3>
          <p class="mt-2 text-sm text-gray-600">
            Free shipping on orders over $50. Fast and reliable delivery worldwide.
          </p>
        </div>

        <div class="text-center">
          <div class="mx-auto flex h-12 w-12 items-center justify-center rounded-md bg-green-500 text-white">
            <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h3 class="mt-4 text-lg font-semibold text-gray-900">Quality Guarantee</h3>
          <p class="mt-2 text-sm text-gray-600">100% satisfaction guarantee. High-quality products you can trust.</p>
        </div>

        <div class="text-center">
          <div class="mx-auto flex h-12 w-12 items-center justify-center rounded-md bg-purple-500 text-white">
            <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M18 18.72a9.094 9.094 0 003.741-.479 3 3 0 00-4.682-2.72m.94 3.198l.001.031c0 .225-.012.447-.037.666A11.944 11.944 0 0112 21c-2.17 0-4.207-.576-5.963-1.584A6.062 6.062 0 016 18.719m12 0a5.971 5.971 0 00-.941-3.197m0 0A5.995 5.995 0 0012 12.75a5.995 5.995 0 00-5.058 2.772m0 0a3 3 0 00-4.681 2.72 8.986 8.986 0 003.74.477m.94-3.197a5.971 5.971 0 00-.94 3.197M15 6.75a3 3 0 11-6 0 3 3 0 016 0zm6 3a2.25 2.25 0 11-4.5 0 2.25 2.25 0 014.5 0zM7.5 3a2.25 2.25 0 11-4.5 0 2.25 2.25 0 014.5 0z" />
            </svg>
          </div>
          <h3 class="mt-4 text-lg font-semibold text-gray-900">24/7 Support</h3>
          <p class="mt-2 text-sm text-gray-600">Round-the-clock customer support to help you with any questions.</p>
        </div>
      </div>
    </div>
  </section>
{% endif %}

<!-- Newsletter Section -->
{% if section.settings.show_newsletter %}
  <section class="py-16 bg-gray-900">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl">
          {{ section.settings.newsletter_title | default: 'Stay Updated' }}
        </h2>
        <p class="mx-auto mt-4 max-w-2xl text-lg text-gray-300">
          {{
            section.settings.newsletter_description
            | default: 'Subscribe to our newsletter for the latest updates and exclusive offers.'
          }}
        </p>
      </div>

      <div class="mt-8 flex justify-center">
        <form class="flex w-full max-w-md gap-x-4" action="{{ routes.cart_url }}" method="post">
          <label for="email-address" class="sr-only">Email address</label>
          <input
            id="email-address"
            name="contact[email]"
            type="email"
            autocomplete="email"
            required
            class="min-w-0 flex-auto rounded-md border-0 bg-white px-3.5 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-white/10 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-white sm:text-sm sm:leading-6"
            placeholder="Enter your email"
          >
          <button
            type="submit"
            class="flex-none rounded-md bg-white px-6 py-2 text-sm font-semibold text-gray-900 shadow-sm hover:bg-gray-100 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
          >
            Subscribe
          </button>
        </form>
      </div>
    </div>
  </section>
{% endif %}

<!-- Schema.org JSON-LD for Website -->
<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "{{ shop.name | escape }}",
    "url": "{{ shop.url }}",
    "description": "{{ shop.description | escape }}",
    "potentialAction": {
      "@type": "SearchAction",
      "target": "{{ shop.url }}/search?q={search_term_string}",
      "query-input": "required name=search_term_string"
    }
  }
</script>

{% schema %}
{
  "name": "Homepage",
  "settings": [
    {
      "type": "header",
      "content": "Hero Section"
    },
    {
      "type": "text",
      "id": "hero_title",
      "label": "Hero Title",
      "default": "Welcome to Our Store"
    },
    {
      "type": "textarea",
      "id": "hero_description",
      "label": "Hero Description",
      "default": "Discover amazing products and exceptional quality. Shop with confidence and enjoy fast, reliable delivery."
    },
    {
      "type": "text",
      "id": "hero_button_text",
      "label": "Primary Button Text",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "hero_button_url",
      "label": "Primary Button URL"
    },
    {
      "type": "text",
      "id": "hero_secondary_button_text",
      "label": "Secondary Button Text",
      "default": "Learn more"
    },
    {
      "type": "url",
      "id": "hero_secondary_button_url",
      "label": "Secondary Button URL"
    },
    {
      "type": "header",
      "content": "Featured Collections"
    },
    {
      "type": "checkbox",
      "id": "show_featured_collections",
      "label": "Show Featured Collections",
      "default": true
    },
    {
      "type": "text",
      "id": "featured_collections_title",
      "label": "Collections Title",
      "default": "Shop by Category"
    },
    {
      "type": "textarea",
      "id": "featured_collections_description",
      "label": "Collections Description",
      "default": "Explore our curated collections"
    },
    {
      "type": "header",
      "content": "Featured Products"
    },
    {
      "type": "checkbox",
      "id": "show_featured_products",
      "label": "Show Featured Products",
      "default": true
    },
    {
      "type": "text",
      "id": "featured_products_title",
      "label": "Products Title",
      "default": "Featured Products"
    },
    {
      "type": "textarea",
      "id": "featured_products_description",
      "label": "Products Description",
      "default": "Check out our best-selling items"
    },
    {
      "type": "collection",
      "id": "featured_collection",
      "label": "Featured Collection"
    },
    {
      "type": "header",
      "content": "Features Section"
    },
    {
      "type": "checkbox",
      "id": "show_features",
      "label": "Show Features",
      "default": true
    },
    {
      "type": "text",
      "id": "features_title",
      "label": "Features Title",
      "default": "Why Choose Us"
    },
    {
      "type": "textarea",
      "id": "features_description",
      "label": "Features Description",
      "default": "We provide exceptional service and quality products"
    },
    {
      "type": "header",
      "content": "Newsletter"
    },
    {
      "type": "checkbox",
      "id": "show_newsletter",
      "label": "Show Newsletter",
      "default": true
    },
    {
      "type": "text",
      "id": "newsletter_title",
      "label": "Newsletter Title",
      "default": "Stay Updated"
    },
    {
      "type": "textarea",
      "id": "newsletter_description",
      "label": "Newsletter Description",
      "default": "Subscribe to our newsletter for the latest updates and exclusive offers."
    }
  ],
  "presets": [
    {
      "name": "Homepage"
    }
  ]
}
{% endschema %}
